const express = require('express');
const router = express.Router();

/**
 * @openapi
 * tags:
 *   name: LABS
 *   description: Decentralized Identifier operations (placeholder endpoints)
 */

/**
 * @openapi
 * /LABS:
 *   get:
 *     summary: Test LABS route
 *     tags: [LABS]
 *     responses:
 *       200:
 *         description: Simple response indicating LABS route works
 *         content:
 *           application/json:
 *             schema:
 *               type: object
 *               properties:
 *                 message:
 *                   type: string
 */

module.exports = router;
